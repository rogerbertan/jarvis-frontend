export const metadata = {
  title: "Analytics - Jarvis",
  description: "Análise financeira detalhada",
};

export default function AnalyticsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
